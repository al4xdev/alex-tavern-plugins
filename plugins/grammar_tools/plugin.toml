schema_version = 1
id = "dev.alex-tavern.grammar-tools"
name = "Grammar Tools"
version = "2.0.0"
description = "Corrects character input grammar through the active structured LLM provider."
license = "MIT"
authors = ["Alex Tavern"]
permissions = ["model.call"]
dependencies = []

[entrypoints]
backend = "backend.py"

[order]
priority = 10
before = []
after = []

[python]
dependencies = []
